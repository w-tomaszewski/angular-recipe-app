import { FilterRecordsPipe } from './filter-records.pipe';

describe('FilterRecordsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecordsPipe();
    expect(pipe).toBeTruthy();
  });
});
